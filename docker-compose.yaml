services:
  postgres:
    image: postgres:17-alpine
    container_name: library-postgres

    environment:
      POSTGRES_DB: ${DB_NAME:-librarymanagementdb}
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-myadmin1234}

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres} -d ${DB_NAME:-librarymanagementdb}"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: library-app

    depends_on:
      postgres:
        condition: service_healthy

    environment:
      DB_URL: jdbc:postgresql://postgres:5432/${DB_NAME:-librarymanagementdb}
      DB_USERNAME: ${DB_USERNAME:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-myadmin1234}
      LIQUIBASE_ENABLED: "true"

    ports:
      - "8080:8080"

volumes:
  postgres-data: